1.nginx指令 -q(Suppress non-error messages during configuration testing) -g(Set global directives.)的用法？
2.nginx -s reopen?没明白reopening the log files的含义？这么做的作用？
3.同上 USR1信号的用法？Reopen the log files的含义作用？
4.“Use try_files if it suits your needs. Use the “return ...” or “rewrite ... last” in other cases. In some cases, it’s also possible to move ifs to server level (where it’s safe as only other rewrite module directives are allowed within it)”
这句话理解？
7.Converting rewrite rules含义即应用
8.怎么发送要进行websocket协议转换的请求？